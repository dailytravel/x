# GraphQL schema example
#
# https://gqlgen.com/getting-started/

type Query
type Mutation

scalar Map

directive @api on FIELD_DEFINITION
directive @auth(roles: [String]) on FIELD_DEFINITION
directive @hasScope(scope: [String!]) on FIELD_DEFINITION
