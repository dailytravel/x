type Token {
  id: ID!
  user: User!
  client: Client!
  token: String!
  expires: String!
  revoked: Boolean!
  last_used: String!
  client_ip: String!
  user_agent: String!
  created: String!
  metadata: Map
  status: String!
}
