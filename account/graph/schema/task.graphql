extend type Task
  @key(fields: "uid")
  @key(fields: "members")
  @key(fields: "assignee") {
  uid: ID! @external
  assignee: ID! @external
  members: [ID!] @external
  user: User
  lead: User #this is assignee
  followers: [User]
}
