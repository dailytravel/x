type Notification {
  id: ID!
  type: String!
  locale: String!
  subject: String!
  body: String!
  read_at: Int
  created_at: String!
  updated_at: String!
  user: User!
  object: Object!
}

type Notifications {
  data: [Notification]
  count: Int!
}

extend type Query {
  notifications(args: Map): Notifications @auth
  notification(id: ID!): Notification! @auth
}

extend type Mutation {
  updateNotification(args: Map!): Map @auth
  deleteNotification(id: ID!): Map @auth
  deleteNotifications(ids: [ID!]!): Map @auth
}
