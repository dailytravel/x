type Membership @key(fields: "id") {
  id: ID!
  user: User!
  tier: Tier! #silver, gold, platinum
  number: String!
  since: String!
  until: String!
  billing: Map
  payment: Map
  transactions: [Transaction]
  metadata: Map
  status: String!
  created_at: String!
  updated_at: String!
}
