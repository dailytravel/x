package graph

// This file will be automatically regenerated based on the schema, any resolver implementations
// will be copied through when generating and any unknown code will be moved to the end.
// Code generated by github.com/99designs/gqlgen version v0.17.36

import (
	"context"
	"errors"
	"fmt"
	"time"

	"github.com/dailytravel/x/sales/graph/model"
	"github.com/dailytravel/x/sales/internal/utils"
	"github.com/dailytravel/x/sales/pkg/auth"
	"go.mongodb.org/mongo-driver/bson"
	"go.mongodb.org/mongo-driver/bson/primitive"
	"go.mongodb.org/mongo-driver/mongo"
)

// CreatePromotion is the resolver for the createPromotion field.
func (r *mutationResolver) CreatePromotion(ctx context.Context, input model.NewPromotion) (*model.Promotion, error) {
	// uid, err := utils.UID(ctx)
	// if err != nil {
	// 	return nil, err
	// }

	item := &model.Promotion{
		Locale:      input.Locale,
		Name:        bson.M{input.Locale: input.Name},
		Description: bson.M{input.Locale: input.Description},
		Model: model.Model{
			Metadata: input.Metadata,
		},
	}

	// Set the fields from the input
	_, err := r.db.Collection(item.Collection()).InsertOne(ctx, item)
	if err != nil {
		return nil, err
	}

	return item, nil
}

// UpdatePromotion is the resolver for the updatePromotion field.
func (r *mutationResolver) UpdatePromotion(ctx context.Context, id string, input model.UpdatePromotion) (*model.Promotion, error) {
	// uid, err := utils.UID(ctx)
	// if err != nil {
	// 	return nil, err
	// }

	// Convert the ID string to ObjectID
	_id, err := primitive.ObjectIDFromHex(id)
	if err != nil {
		return nil, err
	}

	// Create an update document with the fields to be updated
	item := &model.Promotion{}
	filter := bson.M{"_id": _id}
	err = r.db.Collection(item.Collection()).FindOne(ctx, filter).Decode(item)
	if err != nil {
		return nil, err
	}

	if input.Name != nil {
		item.Name[item.Locale] = *input.Name
	}

	if input.Description != nil {
		item.Description[item.Locale] = *input.Description
	}

	if input.Metadata != nil {
		for k, v := range input.Metadata {
			item.Metadata[k] = v
		}
	}

	// Perform the update in the database
	res, err := r.db.Collection(item.Collection()).UpdateOne(ctx, filter, item)
	if err != nil {
		return nil, err
	}

	// Check if the coupon was actually updated
	if res.ModifiedCount == 0 {
		return nil, fmt.Errorf("no coupon was updated")
	}

	return item, nil
}

// DeletePromotion is the resolver for the deletePromotion field.
func (r *mutationResolver) DeletePromotion(ctx context.Context, id string) (*model.Promotion, error) {
	uid, err := utils.UID(ctx)
	if err != nil {
		return nil, err
	}

	// Convert the ID string to an ObjectID
	_id, err := primitive.ObjectIDFromHex(id)
	if err != nil {
		return nil, err
	}

	// Define the filter to match the given ID
	filter := bson.M{"_id": _id}

	// Define the update to mark the record as deleted
	update := bson.M{
		"$set": bson.M{
			"deleted":    primitive.Timestamp{T: uint32(time.Now().Unix())},
			"deleted_by": uid,
			"status":     "deleted",
			"updated_by": uid,
			"updated":    primitive.Timestamp{T: uint32(time.Now().Unix())},
		},
	}

	// Perform the update operation in the database and get the old promotion data
	var oldPromotion model.Promotion
	err = r.db.Collection("promotions").FindOneAndUpdate(ctx, filter, update).Decode(&oldPromotion)
	if err != nil {
		return nil, err
	}

	return &oldPromotion, nil
}

// DeletePromotions is the resolver for the deletePromotions field.
func (r *mutationResolver) DeletePromotions(ctx context.Context, ids []string) (*bool, error) {
	uid, err := utils.UID(ctx)
	if err != nil {
		return nil, err
	}

	// Convert the list of ID strings to ObjectIDs
	var objectIDs []primitive.ObjectID
	for _, id := range ids {
		_id, err := primitive.ObjectIDFromHex(id)
		if err != nil {
			return nil, err
		}
		objectIDs = append(objectIDs, _id)
	}

	// Define the filter to match the given IDs
	filter := bson.M{"_id": bson.M{"$in": objectIDs}}

	// Define the update to mark records as deleted
	update := bson.M{
		"$set": bson.M{
			"deleted":    primitive.Timestamp{T: uint32(time.Now().Unix())},
			"deleted_by": uid,
			"status":     "deleted",
			"updated_by": uid,
			"updated":    primitive.Timestamp{T: uint32(time.Now().Unix())},
		},
	}

	// Perform the update operation in the database
	_, err = r.db.Collection("promotions").UpdateMany(ctx, filter, update)
	if err != nil {
		return nil, err
	}

	// Return a success status
	success := true
	return &success, nil
}

// ID is the resolver for the id field.
func (r *promotionResolver) ID(ctx context.Context, obj *model.Promotion) (string, error) {
	return obj.ID.Hex(), nil
}

// Name is the resolver for the name field.
func (r *promotionResolver) Name(ctx context.Context, obj *model.Promotion) (string, error) {
	// Get the locale from the context
	locale := auth.Locale(ctx)
	if locale == nil {
		locale = &obj.Locale
	}

	// Try to retrieve the name for the requested locale
	if name, ok := obj.Name[*locale].(string); ok {
		return name, nil
	}

	// Return an error if the name is not found for any locale
	return obj.Name[obj.Locale].(string), nil
}

// Description is the resolver for the description field.
func (r *promotionResolver) Description(ctx context.Context, obj *model.Promotion) (string, error) {
	// Get the locale from the context
	locale := auth.Locale(ctx)
	if locale == nil {
		locale = &obj.Locale
	}

	// Try to retrieve the description for the requested locale
	if description, ok := obj.Description[*locale].(string); ok {
		return description, nil
	}

	// Return an error if the description is not found for any locale
	return obj.Description[obj.Locale].(string), nil
}

// Metadata is the resolver for the metadata field.
func (r *promotionResolver) Metadata(ctx context.Context, obj *model.Promotion) (map[string]interface{}, error) {
	return obj.Metadata, nil
}

// Created is the resolver for the created field.
func (r *promotionResolver) Created(ctx context.Context, obj *model.Promotion) (string, error) {
	return time.Unix(int64(obj.Created.T), 0).Format(time.RFC3339), nil
}

// Updated is the resolver for the updated field.
func (r *promotionResolver) Updated(ctx context.Context, obj *model.Promotion) (string, error) {
	return time.Unix(int64(obj.Updated.T), 0).Format(time.RFC3339), nil
}

// Promotion is the resolver for the promotion field.
func (r *queryResolver) Promotion(ctx context.Context, id string) (*model.Promotion, error) {
	var item *model.Promotion

	_id, err := primitive.ObjectIDFromHex(id)
	if err != nil {
		return nil, err
	}

	filter := bson.M{"_id": _id}
	if err := r.db.Collection(item.Collection()).FindOne(ctx, filter).Decode(&item); err != nil {
		if errors.Is(err, mongo.ErrNoDocuments) {
			return nil, fmt.Errorf("document not found")
		}
		return nil, err
	}

	return item, nil
}

// Promotions is the resolver for the promotions field.
func (r *queryResolver) Promotions(ctx context.Context, filter map[string]interface{}, project map[string]interface{}, sort map[string]interface{}, collation map[string]interface{}, limit *int, skip *int) (*model.Promotions, error) {
	var items []*model.Promotion
	//find all items
	cur, err := r.db.Collection("promotions").Find(ctx, nil)
	if err != nil {
		return nil, err
	}

	for cur.Next(ctx) {
		var item *model.Promotion
		if err := cur.Decode(&item); err != nil {
			return nil, err
		}
		items = append(items, item)
	}

	//get total count
	count, err := r.db.Collection("promotions").CountDocuments(ctx, nil)
	if err != nil {
		return nil, err
	}

	return &model.Promotions{
		Count: int(count),
		Data:  items,
	}, nil
}

// Promotion returns PromotionResolver implementation.
func (r *Resolver) Promotion() PromotionResolver { return &promotionResolver{r} }

type promotionResolver struct{ *Resolver }
