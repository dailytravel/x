type Schedule {
  id: ID!
  campaign: ID!
  start: String!
  end: String!
  recurring: Boolean!
  frequency: String
  interval: Int
  days: [String] # array of days of the week
  time: String!
  timezone: String!
  metadata: Map
  createdAt: String!
  updatedAt: String!
}
